# Cython (pxd) generator

Adapted from [pycparser](https://github.com/eliben/pycparser) C Generator

Current Usage:
  ```python c_to_pxd.py input.h output.pxd ```

TODO:
-  add setup.py
-  handle includes
-  add unit tests
